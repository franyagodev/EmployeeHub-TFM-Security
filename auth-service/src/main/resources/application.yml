spring:
  application:
    name: auth-service
  config:
    import: "optional:configserver:http://config-server:8088" # Correcto: usa el nombre del servicio

  datasource:
    url: jdbc:mysql://localhost:3306/auth_db
    username: root
    password: 123456abc
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8071

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/



jwt:
  secret: "Jalrz9_Train!2025carHelicopter_13"
  expiration: 900000
  refresh-expiration: 604800000

